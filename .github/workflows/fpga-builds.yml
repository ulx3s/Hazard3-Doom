name: Board integration builds

on:
  schedule:
    - cron: "23 8 * * 1"
  workflow_dispatch:

permissions:
  # explicit least-privilege setting for GITHUB_TOKEN, no write permissions needed
  contents: read

concurrency:
  group: fpga-builds-${{ github.ref }}
  cancel-in-progress: false

env:
  OSS_CAD_SUITE_VERSION: "2026-07-20"
  XPACK_RISCV_GCC_VERSION: "15.2.0-1"
  XPACK_RISCV_GCC_LINUX_SHA256: "aaaa8060c914851a3e5ee1ba82cc3d6f80972f90638a05c6e823a37557a33758"

jobs:
  build:
    name: ${{ matrix.name }} complete build
    runs-on: ubuntu-24.04
    timeout-minutes: 150
    defaults:
      run:
        shell: bash
    env:
      TOOLCHAIN_PREFIX: ${{ github.workspace }}/bin/riscv-gcc/bin/riscv-none-elf-
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ULX3S 85F
            board: ulx3s-85f
            command: ./scripts/build-ulx3s-doom.sh
            fpga_name: fpga_ulx3s
            monitor_dir: build/ulx3s/monitor
            doom_dir: build/ulx3s/doom-image
            seed_required: false
            boot_init: build/ulx3s/hazard3-boot-monitor.hex
          - name: ULX3S 12F
            board: ulx3s-12f
            command: ./scripts/build-ulx3s-12f-doom.sh
            fpga_name: fpga_ulx3s_12f
            monitor_dir: build/ulx3s-12f/monitor
            doom_dir: build/ulx3s-12f/doom-image
            seed_required: true
            boot_init: build/ulx3s-12f/hazard3-12f-bootstrap.hex
          - name: ULX4M-LD 85F
            board: ulx4m-ld-85f
            command: ./scripts/build-ulx4m-ld-doom.sh
            fpga_name: fpga_ulx4m_ld
            monitor_dir: build/ulx4m-ld/monitor
            doom_dir: build/ulx4m-ld/doom-image
            seed_required: true
            boot_init: build/ulx4m-ld/hazard3-boot-monitor.hex

    steps:
      - name: Check out repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Cache the RISC-V toolchain
        uses: actions/cache@v6
        with:
          path: bin/riscv-gcc
          key: Linux-xpack-riscv-gcc-${{ env.XPACK_RISCV_GCC_VERSION }}

      - name: Install the pinned RISC-V toolchain
        run: |
          install_root="${GITHUB_WORKSPACE}/bin/riscv-gcc"
          gcc="${install_root}/bin/riscv-none-elf-gcc"

          if [[ -d "${install_root}" && ! -x "${gcc}" ]]; then
              echo "Removing incomplete cached toolchain: ${install_root}"
              rm -rf -- "${install_root}"
          fi

          if [[ ! -x "${gcc}" ]]; then
              archive_name="xpack-riscv-none-elf-gcc-${XPACK_RISCV_GCC_VERSION}-linux-x64.tar.gz"
              archive="${RUNNER_TEMP}/${archive_name}"
              url="https://github.com/xpack-dev-tools/riscv-none-elf-gcc-xpack/releases/download/v${XPACK_RISCV_GCC_VERSION}/${archive_name}"

              curl --fail --location --retry 3 --output "${archive}" "${url}"
              printf '%s  %s\n' "${XPACK_RISCV_GCC_LINUX_SHA256}" "${archive}" | sha256sum --check -

              mkdir -p -- "${install_root}"
              tar -xzf "${archive}" --strip-components=1 -C "${install_root}"
          fi

          "${gcc}" --version

      - name: Set up the pinned OSS CAD Suite
        uses: YosysHQ/setup-oss-cad-suite@v4
        with:
          version: ${{ env.OSS_CAD_SUITE_VERSION }}
          github-token: ${{ github.token }}
          python-override: true

      - name: Show tool versions
        run: |
          make --version
          yosys --version
          nextpnr-ecp5 --version
          printf 'ecppack: %s\n' "$(command -v ecppack)"
          "${TOOLCHAIN_PREFIX}gcc" --version
          python3 --version

      - name: Initialize required submodules
        run: |
          # refresh submodules and show branch status in build/source_status.log
          ./scripts/setup-submodules.sh
          ./scripts/hazard3-doom-source-status.sh

          # Hazard3 also has submodules that may be interesting, but not directly related
          ./scripts/hazard3-submodule.sh status

      - name: Build FPGA, monitor, and Doom image
        env:
          FORCE_BITSTREAM_REBUILD: "1"

        run: |
          # Produce a fresh P&R result. All release board builds must close
          # timing; exploratory timing-allow-fail runs belong in sweep jobs.
          ${{ matrix.command }}

          test -s "build/${{ matrix.fpga_name }}.bit"
          test -s "build/${{ matrix.fpga_name }}.config"
          test -s "build/${{ matrix.fpga_name }}.svf"
          test -s "build/${{ matrix.fpga_name }}.pnr.log"
          test -s "build/${{ matrix.fpga_name }}.synth.log"
          if [[ "${{ matrix.seed_required }}" == "true" ]]; then
              test -s "build/${{ matrix.fpga_name }}.seed"
          fi
          test -s "${{ matrix.monitor_dir }}/hazard3-boot-monitor.elf"
          test -s "${{ matrix.doom_dir }}/hazard3-doom.h3d"
          test -s "${{ matrix.boot_init }}"

      - name: Upload build artifacts and timing logs
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.board }}-${{ github.sha }}
          path: |
            build/${{ matrix.fpga_name }}.*
            build/source_status.log
            ${{ matrix.monitor_dir }}/hazard3-boot-monitor.elf
            ${{ matrix.monitor_dir }}/hazard3-boot-monitor.map
            ${{ matrix.monitor_dir }}/hazard3-boot-monitor.bin
            ${{ matrix.doom_dir }}/hazard3-doom.elf
            ${{ matrix.doom_dir }}/hazard3-doom.map
            ${{ matrix.doom_dir }}/hazard3-doom.h3d
            ${{ matrix.boot_init }}
          if-no-files-found: warn
          retention-days: 14
          compression-level: 0
